<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DateActivity">

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/date_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Zbyt nowa wersja Androida"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:id="@+id/linear_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/logo_imageview"
            android:layout_width="128dp"
            android:layout_height="128dp"
            android:src="@drawable/ic_archlinux_icon_crystal_64dp"
            android:contentDescription="arch_logo.svg"
            tools:src="@drawable/ic_trophy_128dp" />

        <ImageView
            android:id="@+id/stand_imageview"
            android:layout_width="192dp"
            android:layout_height="96dp"
            android:src="@drawable/ic_stand_192dp"
            android:contentDescription="Pierwsze wydanie najlepszej dystrybucji Linuxa na świecie" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>